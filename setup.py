
from setuptools import setup
from setuptools.extension import Extension

setup(
    name='biasadaptation',
    packages=['biasadaptation',
              'biasadaptation.utils',
              'biasadaptation.weightmatrices',
              'biasadaptation.biasfit']
)
